[package]
name = "spice-core"
version = "0.0.0"
edition = "2021"
description = "Dialect-agnostic SPICE netlist lexer/parser/symbol-table core (ngspice, Xyce, later a reference tool)."
license = "AGPL-3.0-or-later"
publish = false

[lib]
name = "spice_core"

[dependencies]

[dev-dependencies]
