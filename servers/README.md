# servers/

Language server implementation(s) for SPICE netlists.

Implementation language/runtime is not yet decided — to be discussed once
`docs/GRAMMAR.md` is complete. Candidates under consideration: TypeScript/Node
(shares tooling with the extension), Python (pygls), Rust, Go.
