## 2026-08-28 16:00 — Audit doc coverage for `general-spice-core`; fix stale intra-doc link

Companion pass to the same audit done in `general-mna`: the user wants both libraries'
documentation (`cargo doc` + README) complete enough that a reader never has to open the
source. `general-spice-core`'s `src/lib.rs` already had `#![warn(missing_docs)]` in source
(unlike `general-mna`, which only just gained it this session) — rebuilding with a forced
`touch` to bypass caching confirmed genuinely zero missing-docs warnings, and `cargo doc
--no-deps --lib` found exactly one issue: `src/ast.rs`'s doc comment on `Statement::Block`
linked to `crate::parser::ngspice::parse_line`/`crate::parser::xyce::parse_line`, but both
functions are actually named `parse` (stale reference, likely left over from a rename).
Fixed both links to point at the real function names — both modules are `pub`, so these now
resolve as real, working intra-doc links, not plain text.

README.md (root and `servers/core/README.md`) were already thorough — runnable examples
covering single-file parsing, diagnostics via `symbols::*`, expression parsing for both
dialects, and multi-file `.include` resolution, plus a "which crate is right for your use
case" section in the crate-level doc comment. Nothing else needed changing here.

Verification: `cargo doc --no-deps --lib` clean (0 warnings). `cargo fmt --all -- --check`,
`cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` (383
unit tests + 7 block-instance tests + 3 real-netlist conformance tests + 4 doctests, all
passing) all clean.

## 2026-08-28 15:41 — Genericize named-competitor mentions; add this journal

The user asked to strip mentions of the actual proprietary SPICE-dialect vendor this project
plans to eventually support ("no exceptions") from both the working tree and git history, and
to verify no trace of it (or "competitor") remained anywhere.

Reworded every mention across README.md, `servers/core/{Cargo.toml,README.md,src/lib.rs}`,
`docs/GRAMMAR.md` (including the whole §11 placeholder section, which also named the vendor
outright), and `progress.core.yaml` to a generic "third proprietary SPICE dialect". Also
genericized a Xyce test fixture's own provenance comments (a third-party dual-active-bridge
schematic translation that named the tool it was originally authored in and a specific
proprietary file extension) and a `NOTICE.md` attribution bullet, keeping the substantive
license/provenance content intact. Added the same local `scripts/style-guard.sh` +
pre-commit-hook check that `general-mna`/`general-simulator` already carry, wired under a
neutral hook id so a public clone doesn't reveal what it's specifically checking for.

Verification: recursive case-insensitive grep of the working tree for the vendor name, its
common misspellings/variants, and the other named tools this "no exceptions" sweep also
covered (the block-diagram tool and the two power-electronics-focused proprietary simulators)
turned up nothing. History verification required care — `git log -S<term>` (pickaxe) only
flags commits where a string's *occurrence count* changes, not every commit whose tree
contains it, so it silently misses commits where the string was merely present unchanged. Used
`git grep` against every commit's full tree (`git rev-list --all`) instead, across all three
repos (`general-mna`, `general-simulator`, `spice-lsp`): 0 hits. Also checked stashes, all
reflogs, and dangling objects (`git fsck --unreachable`); found one dangling commit here
(`ec74cd7`, an artifact of amending this same genericization commit) whose tree was already
clean, then removed it with `git gc --prune=now` (default `git gc` only prunes unreachable
objects after a 2-week grace period, so a plain `git gc` first left it in place).

Also added this `docs/journal/` (present in `general-mna` and `general-simulator` but never
set up here) at the user's request, mirroring their format exactly.

Remaining: none for the genericization/verification task itself. The new §11 placeholder in
`docs/GRAMMAR.md` still needs real reference material before that section can be written from
verified docs rather than general knowledge.
